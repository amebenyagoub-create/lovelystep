{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo 'cron service: no build needed'"
  },
  "deploy": {
    "startCommand": "node scripts/cron-meta-sync.mjs",
    "cronSchedule": "0 4 * * *",
    "restartPolicyType": "NEVER"
  }
}
